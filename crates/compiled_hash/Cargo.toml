[package]
name = "compiled_hash"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Deterministic hashing utilities shared across Compiled SWC crates"

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
